{
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && cp -r build/* /your/web/host/path/",
    "test": "react-scripts test",
    "lint": "eslint src"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    // ...other deps as listed above
  }
}